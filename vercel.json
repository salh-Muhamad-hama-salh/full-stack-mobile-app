{
  "buildCommand": "npm run build",
  "outputDirectory": "admin/dist",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
